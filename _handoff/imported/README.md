# imported

Raw files from another agent, another chat, or another vault.

Nothing here applies to this vault until it is approved. Log each item in
`_handoff/IMPORTED_HANDOFFS.md` inside the pending-review markers, then wait for
the user. See the Explicit Confirmation Gate in `CLAUDE.md`.
